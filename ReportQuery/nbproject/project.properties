annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.run.all.processors=true
application.args=
application.title=ReportQuery
application.vendor=americuz_pu
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/ReportQuery.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
file.reference.HospitalOSV3.jar=D:\\New Folder\\HospitalOSV3\\dist\\HospitalOSV3.jar
file.reference.Report.jar=D:\\New Folder\\Report\\dist\\Report.jar
file.reference.ReportQuery-src=src
file.reference.standardHospitalOS.jar=D:\\New Folder\\standardHospitalOS\\dist\\standardHospitalOS.jar
includes=**
jar.compress=false
javac.classpath=\
    ${libs.Hos39.classpath}:\
    ${libs.ireport.classpath}:\
    ${file.reference.Report.jar}:\
    ${file.reference.HospitalOSV3.jar}:\
    ${file.reference.standardHospitalOS.jar}
# Space-separated list of extra javac options
javac.compilerargs=-deprecation
javac.deprecation=false
javac.processorpath=\
    ${javac.classpath}
javac.source=1.4
javac.target=1.4
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=com.reportcenter.MainReport
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=${file.reference.ReportQuery-src}
