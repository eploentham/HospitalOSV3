//ย้ายไปไว้ใน Standard แล้วนะ henbe//ย้ายไปไว้ใน Standard แล้วนะ henbe//ย้ายไปไว้ใน Standard แล้วนะ henbe//ย้ายไปไว้ใน Standard แล้วนะ henbepackage com.hosv3.object;import com.hospital_os.usecase.connection.CommonInf;import com.hospital_os.object.*;public class Employee2 extends Employee implements CommonInf{	static final long serialVersionUID = 0;	public String default_tab;        public Employee2(){    }    public Employee2(Employee e){        employee_id= e.employee_id;        password= e.password;        fname= e.fname;        lname= e.lname;        employee_no= e.employee_no;        last_login= e.last_login;        last_logout= e.last_logout;        active= e.active;        default_service_id= e.default_service_id;        level_id= e.level_id;        rule_id= e.rule_id;        authentication_id= e.authentication_id;        warning_dx= e.warning_dx;        default_tab = "";    }    public String getCode() {        return this.getObjectId();    }        public String getName() {        return this.fname + "  " + this.lname;    }    public String toString(){        return getName();    }   /**    * @roseuid 3F658BBB036E    */}