/* * CryptPassword.java * * Created on 25 มิถุนายน 2547, 11:33 น. */package com.hospital_os.utility;import java.security.*;/** * * @author  Administrator */public class CryptPassword {        /** Creates a new instance of CryptPassword */    public CryptPassword() {    }    /*เข้ารหัสแบบ MD 5*/    public static String encryptText(String sign)    {	StringBuffer hexString = new StringBuffer();	try 			{            MessageDigest md = MessageDigest.getInstance("MD5");            md.update(sign.getBytes());            byte[] hash = md.digest();            for (int i=0;i<hash.length;i++)             {	if ((0xff & hash[i]) < 0x10)                    hexString.append("0" + Integer.toHexString((0xFF & hash[i])));		else		    hexString.append(Integer.toHexString(0xFF & hash[i]));            }        } 	catch (NoSuchAlgorithmException nsae) 	{nsae.printStackTrace(Constant.getPrintStream()); }	return  hexString.toString();    }        public static void main(String[] args)    {        System.err.println(encryptText("123456"));    }}